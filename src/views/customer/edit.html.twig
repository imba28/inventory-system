{% extends 'base/layout.html.twig' %}

{% block content %}
    <div class="container-fluid">
        <div class="row">
            <nav class="col-sm-3 col-md-2 d-none d-sm-block bg-light sidebar pt-3">
                <ul class="nav nav-pills flex-column">
                    <li class="nav-item">
                        <a class="nav-link" href="/customers">Übersicht</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/customers/new">Kunde anlegen</a>
                    </li>
                </ul>
            </nav>
            <main class="col-sm-9 ml-sm-auto col-md-10 pt-3" role="main">
                {% import 'partials/customer-form.html.twig' as customer_form %}
                {{ customer_form.render(customer, (customer.get('name')|default(customer.get('internal_id') ) ~ ' &#187; Bearbeiten')|raw, 'Speichern', '/customer/' ~ customer.getId(), 'put') }}
            </main>
        </div>
    </div>
{% endblock %}